import React from "react";
import PropTypes from "prop-types";

const BlogInteracciones = ({ Interacciones = [] }) => {
  return <div>Interacciones</div>;
};

BlogInteracciones.propTypes = {
  Interacciones: PropTypes.array,
};

export default BlogInteracciones;
